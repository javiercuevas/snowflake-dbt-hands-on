name: snowflake_dbt_hands_on
version: 1.0.0
config-version: 2
profile: snowflake_dbt_hands_on
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  snowflake_dbt_hands_on:
    +materialized: view
